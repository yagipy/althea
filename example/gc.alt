struct User {
    id: u64,
}

func main() u64 {
    let user: User = User {
        id: 1,
    }
    let result: u64 = 1 //do_something(user)
    result
}

func do_something(user: User) u64 {
    let tmp_user: User = User {
        id: 2,
    }
    tmp_user.id + user.id
}
