struct User {
    id: i32,
}

func main() i32 {
    let user: User = User {
        id: 1,
    }
    let result: i32 = 1 //do_something(user)
    result
}

func do_something(user: User) i32 {
    let tmp_user: User = User {
        id: 2,
    }
    tmp_user.id
}
