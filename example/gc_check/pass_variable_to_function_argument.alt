struct User {
    id: i32,
}

func main() i32 {
    let user: User = User {
        id: 1,
    }
    let num: i32 = foo(user) // 借用
    user.id
}

func foo(arg_user: User) i32 {
    let tmp_user: User = User {
        id: 2,
    }
    // tmp_userは解放される
    0
}
